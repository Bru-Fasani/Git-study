git init

git clone

git remote add origin "URL"
git remote -v

git push origin
git push -u origin

git pull origin

git fetch

git status

git config --global user.name "your name"
git config --global user.email"your@email.com"

git add 
git add.

git commit -m
git commit -am

git log

git diff

git branch
git branch -d
git branch -D

git checkout
git checkout -b
git merge

git restore 
git reset --hard
git reset --soft HEAD~1
git reset --hard HEAD~1
git revert <hash-do-commit>

git stash 
git stash list
git stach apply
git stach drop

git tag
git tag -a v1.0 "versão 1.0"
git push origin --tags